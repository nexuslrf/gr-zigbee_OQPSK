# auto-generated by grc.converter

id: oqpsk_dsss_packet_sink
label: Packet Sink
category: '[OQPSK_DSSS]'

parameters:
-   id: threshold
    label: Threshold
    dtype: int
    default: '10'
-   id: dsss_mode
    label: Dsss mode
    dtype: int
    default: '32'
-   id: verbose
    label: Verbose
    dtype: int
    default: '0'
-   id: verbose2
    label: Verbose2
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: float
    multiplicity: '1'

outputs:
-   domain: message
    id: out

templates:
    imports: import oqpsk_dsss
    make: oqpsk_dsss.packet_sink(${threshold}, ${dsss_mode}, ${verbose}, ${verbose2})

file_format: 1
